# Copy to live.backend.hcl, replace every placeholder, and keep that file uncommitted.
bucket       = "replace-with-live-state-bucket"
key          = "whox-treasury/live/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
kms_key_id   = "replace-with-live-state-kms-key-arn"
